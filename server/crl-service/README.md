# CRL Service Implementation

A demo Certificate Revocation List (CRL) service showcasing two new revocation
models: domain-bound CRL (DB-CRL) and mini CRL (mCRL).

Roadmap:

- unit tests
- end to end tests
