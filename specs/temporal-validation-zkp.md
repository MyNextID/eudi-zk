# Temporal validation within ZKPs

Summary: instead of proving a VC is valid at a time "t" we can prove VC is valid
within a time window \[t_min, t_max\]. This enables the verifier to validate a
signature at any time within the time range.
